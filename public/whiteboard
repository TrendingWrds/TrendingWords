nlp

  $.get('/api/badwords').then(results => {
    let badwords = results;
    console.log('this is our array of bad words ' + badwords);
  });

server

APP.get('/api/badwords', function(request, response){
  response.send(array);
});
